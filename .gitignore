# 모든 env 변형(.env.production 등 포함) — 시크릿 커밋 방지
.env*
.DS_Store
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.branches/
.temp/
.omc/
# 루트 기획 문서(API_SPEC·ERD)만 제외 — frontend/docs(policy.md)는 추적 유지
/docs/
# 디자인 원본(Pencil .pen + 원본 에셋 20MB)은 레포에 넣지 않는다.
# 랜딩이 실제로 쓰는 이미지는 frontend/images/landing/ 로 복사되어 추적된다.
/frontend/design/
